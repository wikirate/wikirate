'use strict';

var _coffeescript = require('coffeescript');

exports.process = (source, file) => {
  var transpile;
  if (!_coffeescript.helpers.isCoffee(file)) {
    return source;
  }
  if (_coffeescript.helpers.isLiterate(file)) {
    source = _coffeescript.helpers.invertLiterate(source);
  }
  transpile = {
    plugins: ['transform-es2015-modules-commonjs'],
    presets: ['jest']
  };
  return (0, _coffeescript.compile)(source, {
    bare: true,
    inlineMap: true,
    transpile
  });
}; // Generated by CoffeeScript 2.0.0