.source-selector{ class: ("no-citation" if card.year == "false") }
  - if new_source_form?
    = render :new_source_form
  - else
    = render :sourcebox
    - if source_results?
      = render :source_results
    - else
      = render :my_sources
      = render :suggested_sources
