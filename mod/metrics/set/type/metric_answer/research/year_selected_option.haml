.d-flex.flex-nowrap.align-items-center
  .image-box.small.bg-white.text-muted
    = mapped_icon_tag :year
  %h6.mb-0
    - if card.year.present?
      %strong
        = card.year
    - else
      %span.text-muted
        Select Year