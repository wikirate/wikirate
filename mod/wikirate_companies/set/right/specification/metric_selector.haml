._metric-selector.metric-selector.form-control.ellipsis
  = modal_link (metric&.name || "Choose Metric"),
               class: "_selectable-filter-link",
               path: { mark: :metric,
                       view: :selectable_filtered_content,
                       slot: { items: { view: :bar } } }
  = hidden_field_tag "#{filter_prefix}[metric_id]", metric&.id