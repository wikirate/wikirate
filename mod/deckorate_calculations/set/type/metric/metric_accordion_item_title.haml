.metric-accordion-item-title.row.w-100
  .metric-accordion-thumbnail{ class: answer ? "col-6" : "col-9" }
    = render_thumbnail
  .col-2.d-flex{ class: "justify-content-#{answer ? 'center' : 'end'}" }
    = detail
  - if answer
    .col-3
      = answer