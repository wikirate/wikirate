.thumbnail{ data: wrap_data(false) }
  = render_thumbnail_image
  .thumbnail-text
    .thumbnail-title{ title: card.name }
      = thumbnail_title
    .thumbnail-subtitle.small.text-muted
      = thumbnail_subtitle